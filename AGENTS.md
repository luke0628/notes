# Lu's Workspace — Agent Conventions

> Auto-injected into every session whose working directory is under this tree.
> Keep it tight. Update as conventions evolve.

## Who

Lu (kinglu628) — Juniper TAC engineer, PTX/MX/ACX, JunOS/JunOS-EVO/VMHost.
GitHub: luke0628. Discord: kinglu628.

## Platforms

| Machine | OS | Notes |
|---------|-----|-------|
| Desktop | Fedora/Bazzite Kinoite | Primary. SELinux enforced. |
| MacBook | macOS (corporate) | Behind Zscaler SSL inspection. certifi cacert.pem needs Zscaler root cert. api.deepseek.com blocked — use OpenRouter. |

## Communication

- **Concise.** One-liners preferred. No fluff.
- **Impatient.** Long debugging → go nuclear (reinstall). Keep it tight.
- **English or Chinese.** Match the user's language.

## Code Style

- Python 3.12+. Type hints. `black` (line-length=100).
- Git: `feat/*`, `fix/*`, `refactor/*` branches.
- Commit messages: conventional commits (`feat:`, `fix:`, `docs:`, `refactor:`).
- Tests: pytest. Write them.

## Tools & Scripts

- `~/clip` — LAN clipboard (Python TCP). `clip listen` on relay, `CLIP_HOST=<ip> clip copy "text"` on client.
- `~/juniper-commands.md` — Juniper command reference (auto-generated by monitor).
- `~/.hermes/scripts/juniper_monitor.py` — RSI file monitor.

## Multi-Session Coordination

### Session = Thread
Each Discord thread is an independent session. Context, todo, and terminal are isolated.
Only memory, skills, and cron jobs span sessions.

### Parallel Work Pattern
For multi-thread development:
1. One thread = one feature branch (`git checkout -b feat/X`)
2. Code sync via Git — commit + push from each thread
3. Conventions via memory + this AGENTS.md — all threads load the same rules
4. Integration: merge branches in a dedicated thread, run full tests

### Cross-Machine Sync
All workflows, skills, and scripts live in GitHub repos:
- `hermes_juniper` (private) — Juniper TAC tools
- Clone on any machine: `git clone`, copy skill to `~/.hermes/skills/`, copy scripts to `~/.hermes/scripts/`

### Skill Inventory
Load on demand via `skill_view()`:
| Skill | When |
|-------|------|
| `juniper-support` | Juniper RSI diagnosis, case workflow |
| `collaboration-framework` | Multi-session/thread coordination |
| `cross-machine-sync` | Sync setup between machines |
| `github-pr-workflow` | PR lifecycle |
| `github-code-review` | Code review |
| `linux-input-method` | fcitx5/IBus config |
| `linux-power-settings` | Sleep/power on Linux |
| `stock-monitor` | UNH stock monitoring |

### Cron Jobs (autonomous)
- Juniper RSI Monitor: `every 6h`, scans `~/Downloads/` for new RSI files
- Stock monitoring: breaking news /30min, digest /5h, financials daily 9AM

## Juniper TAC Conventions

- RSI diagnosis: user names the file + timestamp + issue type. I locate in RSI, read ±5-10min window only. Never scan entire /var/log.
- Commands get cataloged by scenario (bgp, ospf-isis, interface-optics, etc.).
- Token budget is a hard constraint — surgical, not data dump.
